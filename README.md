# Word Speaker

A tiny Windows desktop app to import a word list (txt/csv) and click to hear pronunciation.

## Run

`powershell
pip install -r requirements.txt
python app.py
`

## Input format

- .txt: one word per line
- .csv: use first column as word
